# =============================================================================
# MCP Multi-Component Server Configuration
# Copy this file to .env and update with your values
# =============================================================================

# -----------------------------------------------------------------------------
# GitHub Configuration (for publishing to ghcr.io)
# -----------------------------------------------------------------------------
# Create a Personal Access Token at: https://github.com/settings/tokens
# Required scope: write:packages
GITHUB_TOKEN=your_github_token_here
GITHUB_USER=your_github_username

# -----------------------------------------------------------------------------
# Cosmonic Configuration (required for Kubernetes deployment)
# -----------------------------------------------------------------------------
# Get your license key from: https://cosmonic.com
COSMONIC_LICENSE_KEY=your_cosmonic_license_key_here

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Application name (used for deployments, services, etc.)
APP_NAME=mcp-multi-tools

# Version tag for builds and deployments
VERSION=0.2.0

# -----------------------------------------------------------------------------
# Container Image Configuration
# -----------------------------------------------------------------------------
# Image base (repository without tag)
# Default: ghcr.io/wasmcp/example-mcp (public demo image)
# Set to your own: ghcr.io/YOUR_USER/YOUR_REPO
IMAGE_BASE=ghcr.io/wasmcp/example-mcp

# Full image reference (optional - overrides IMAGE_BASE:VERSION)
# Use this to specify a complete image reference including tag
# Example: ghcr.io/myuser/myimage:custom-tag
# IMAGE=

# -----------------------------------------------------------------------------
# Cluster Configuration (for Cosmonic deployment)
# -----------------------------------------------------------------------------
# Kind cluster name
CLUSTER_NAME=cosmonic-cluster

# Kubernetes namespace for deployments
NAMESPACE=default

# -----------------------------------------------------------------------------
# Local Development Configuration (for wash runtime)
# -----------------------------------------------------------------------------
# HTTP server port for local wash development
DEV_PORT=8080
